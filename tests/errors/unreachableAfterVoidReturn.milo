// @error: unreachable code
// A bare `return` in a void fn used to swallow the next line as its value, so the
// call still ran (and the block was left with instructions past its terminator).

fn f(x: i64) {
    print(x)
    return
    print(x + 1)
}

pub fn main(): i32 {
    f(3)
    return 0
}
