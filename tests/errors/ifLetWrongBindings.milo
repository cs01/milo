// @error: variant 'Some' has 1 fields, but pattern has 2 bindings

enum Maybe {
    Some(i32),
    None,
}

fn main(): i32 {
    let x = Maybe.Some(42)
    if let Maybe.Some(a, b) = x {
        return 0
    }
    return 0
}
