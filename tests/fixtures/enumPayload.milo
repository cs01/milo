// @expect: 42

pub enum Maybe {
    Some(i32),
    None,
}

pub fn main(): i32 {
    let x = Maybe.Some(42)
    match x {
        Maybe.Some(val) => {
            print(val)
        }
        Maybe.None => {
            print("none")
        }
    }
    return 0
}
