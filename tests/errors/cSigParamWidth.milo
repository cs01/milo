// @skip-os: win32
// asserts on a diagnostic that quotes stdlib.h's abs spelling.

// @error: Milo declares i64 (8 bytes)
// A scalar parameter declared too wide is an ABI mismatch the linker cannot see.
@cSig("stdlib.h", "int abs(int)")
extern fn abs(x: i64): i32

fn main() {
    print(1)
}
